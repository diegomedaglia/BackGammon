<UserControl
    x:Class="BackGammon.BGPoint"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BackGammon"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="100" Padding="0,0,0,0" RenderTransformOrigin="0.5,0.5"
    x:Name="BGPointcontrol"
    >
    <UserControl.Resources>
        <local:BoolToRotationConverter x:Key="BTRC" />
    </UserControl.Resources>
    
    <Canvas>
        <Polygon Points="0,0,50,160,100,0" Fill="{Binding TriangleFill, ElementName=BGPointcontrol}" Stroke="Black" />
        <Rectangle Canvas.ZIndex="1" Fill="Transparent" />
    </Canvas>

    <UserControl.RenderTransform>
        <CompositeTransform Rotation="{Binding Path=BGPointControl.TopRow, Converter={StaticResource BTRC}}" />
    </UserControl.RenderTransform>

</UserControl>
